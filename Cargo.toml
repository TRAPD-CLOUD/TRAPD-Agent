[workspace]
# trapd-agent-ebpf lives outside this workspace: it targets bpfel-unknown-none
# and requires a different toolchain (nightly + bpf-linker). Build it with:
#   cargo xtask build-ebpf --release
members  = ["agent", "xtask"]
resolver = "2"
